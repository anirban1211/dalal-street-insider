services:
  - type: web
    name: dalal-street-insider
    runtime: node
    buildCommand: npm install
    startCommand: node backend/server.js
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: NODE_ENV
        value: production
